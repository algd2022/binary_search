#include <stdio.h>

int n;
int k;
int A[100000];

int p(int x){
    int sum = 1, p = 0, m = 1;
    for(int m=0; m < n; m++)
        if(p z + a[m]<=x){p = p + x}
        else if(x < A[m]){m = 0;}
        else {p = A[m], sum++}
}
    return sum<=k && m

int main(){
  int i, lb, ub;
  scanf("%d%d", &n, &k);
  for(i = 0; i < n; i++){
    scanf("%d", &A[i]);
  }


  return 0;
}
